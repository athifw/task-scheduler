<%= form_for @room do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<% if @room.errors.any? %>
  <% @room.errors.full_messages.each do |error| %>
    <%= error %><br>
  <% end %>
<% end %>
